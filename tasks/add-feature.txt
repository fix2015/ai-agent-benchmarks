Add a rate limiting middleware to the Express.js API. Requirements:
- Limit each IP to 100 requests per 15-minute window
- Return 429 Too Many Requests with a Retry-After header when exceeded
- Store counters in Redis (connection string from REDIS_URL env var)
- Skip rate limiting for requests with a valid API key in the X-API-Key header
- Add unit tests for the middleware
- Update the README with rate limiting documentation
